apiVersion: v1
kind: Service
metadata:
  name: vaultwarden
  namespace: applications
  labels:
    app.kubernetes.io/name: vaultwarden
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
    - name: websocket
      port: 3012
      targetPort: websocket
  selector:
    app.kubernetes.io/name: vaultwarden
